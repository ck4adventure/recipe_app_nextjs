// recipes data

// categories table should exist
// columns id, name 
// name is required and unique

// recipes table should exist
// columns id, slug:text, title:text
// title is required, slug is required and unique

// recipe_categories table should exist
// columns id, recipe_id, category_id
// recipe_id and category_id are required