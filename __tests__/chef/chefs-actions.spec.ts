// tests next.js actions for chefs page
import { expect as jestExpect } from '@jest/globals';

// testing chefs page server actions
describe('chefs page actions', () => {
	it('should pass', () => {	
		jestExpect(true).toBe(true);
	});
});
