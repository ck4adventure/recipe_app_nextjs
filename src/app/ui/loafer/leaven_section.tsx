'use client'
import { useState } from "react"

// LeavenFormSection only deals with the data timestemps for creating the leaven
export const LeavenFormSection = (data: any) => {
	return (
		<div></div>
	);
}